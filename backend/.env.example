# Backend Environment Variables

# Node Environment
NODE_ENV=development

# Server Port
PORT=4000

# Database (PostgreSQL - Neon/Vercel Postgres)
# Use a URL de POOL para o datasource do Prisma (melhor para produção)
POSTGRES_PRISMA_URL="postgres://USER:PASSWORD@HOST:PORT/DB?sslmode=require&pgbouncer=true&connection_limit=1"

# Use a URL DIRETA (sem pool) para comandos administrativos (migrate/studio)
POSTGRES_URL_NON_POOLING="postgres://USER:PASSWORD@HOST:PORT/DB?sslmode=require"

# JWT Secret (gere uma chave segura em produção)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Frontend URL (para CORS)
FRONTEND_URL=http://localhost:5173

# Auto-seed (opcional): cria admin e 5 usuários caso o banco esteja vazio
# ATENÇÃO: só habilite em ambientes controlados. Em produção, defina explicitamente.
AUTO_SEED=false
ADMIN_EMAIL=admin@leadcampanha.com
ADMIN_PASSWORD=admin123

# WhatsApp Cloud API (Meta Business)
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_VERIFY_TOKEN=lead_campanha_webhook_token_2025

# CORS / Origins
# Se seu frontend está hospedado em Vercel (ex: https://velsrios.vercel.app), defina FRONTEND_URL
# para esse domínio. Você também pode fornecer origens adicionais separadas por vírgula:
# EXTRA_ALLOWED_ORIGINS="https://velsrios.vercel.app,https://outro.dominio.com"
# Em caso de debug temporário, habilite ALLOW_ALL_ORIGINS=true
EXTRA_ALLOWED_ORIGINS=
ALLOW_ALL_ORIGINS=false

# n8n Cloud Integration
N8N_WEBHOOK_URL=
N8N_API_KEY=
