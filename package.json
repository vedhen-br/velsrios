{
  "name": "lead-campanha",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "prisma:generate": "npm run prisma --prefix backend -- generate",
    "prisma:migrate": "npm run prisma --prefix backend -- migrate dev",
    "prisma:studio": "npm run prisma --prefix backend -- studio",
    "seed": "npm run seed --prefix backend",
    "codespaces": "echo '🚀 Iniciando Lead Campanha CRM no Codespaces...' && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
